# design — <feature>

## Approach

(How it works, in a few paragraphs or a short diagram.)

## Modules touched

(domain / application / infrastructure / presentation / src — mark new crossings; Rust↔TS crossings are what forced Lane 2.)

## Data model changes

(Tables, migrations, schema ownership — reference ADR 0003 contracts.)

## Alternatives considered

(At least one, with the reason it was rejected.)

## ADR

(Link if load-bearing; otherwise "none". ADRs are the only decision log — don't restate the spec there.)
