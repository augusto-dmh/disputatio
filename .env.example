# Copy to .env before running docker compose up (see docker/docker-compose.yml, ADR 0003)
POSTGRES_USER=disputatio
POSTGRES_PASSWORD=change-me
POSTGRES_DB=disputatio
# Host port; 5433 avoids clashing with other local Postgres instances (e.g. learny)
PGPORT=5433
